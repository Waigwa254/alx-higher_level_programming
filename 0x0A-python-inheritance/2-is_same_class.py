#!/usr/bin/python3
"""Defineg function."""


def is_same_class(obj, a_class):
    """Cht is exactly an instance of a given class.

    Args:
        oobject to check.
        a the type of obj to.
    Returns:
        Ily an instance of a_class - True.
        Oe - False.
    """
    if type(obj) == a_class:
        return True
    return False
